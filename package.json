{
  "name": "thiepn-convert",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:engines": "node scripts/copy-vips-assets.mjs",
    "predev": "npm run prepare:engines",
    "dev": "vite",
    "prebuild": "npm run prepare:engines",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "wasm-vips": "0.0.18"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.0",
    "vitest": "^3.2.4"
  }
}
